These fixture artifacts are representative Aztec-style contract artifacts used for
serialization, ABI loading, and example coverage.

They are intentionally small and stable for tests in this repository. They are
not copied verbatim from a real compiler output yet, so they should be treated
as compatibility fixtures rather than canonical upstream artifacts.
