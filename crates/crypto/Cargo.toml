[package]
name = "aztec-crypto"
version = "0.3.0"
edition = "2021"
description = "Cryptographic primitives and key derivation for the Aztec Rust SDK"
license = "Apache-2.0"

[dependencies]
aztec-core = { path = "../core", version = "0.3.0" }
sha2 = "0.10"
