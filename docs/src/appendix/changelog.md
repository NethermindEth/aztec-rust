# Changelog

The complete project changelog is embedded below.
The format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/), and versions follow [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Per-crate changes are tagged inline with the affected crate (e.g. `(aztec-ethereum)`).
Until individual crates ship separate changelogs, this file is the authoritative record for all workspace members.

---

{{#include ../../../CHANGELOG.md}}
