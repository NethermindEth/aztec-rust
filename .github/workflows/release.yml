name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  check:
    name: Pre-release checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo lint
      - run: cargo test --all-features

  # crates.io publishing is disabled while we depend on an unpublished
  # Noir pre-release (see root Cargo.toml [patch.crates-io] for pinned
  # git revisions of acvm/brillig_vm/acir/noirc_abi/bn254_blackbox_solver).
  # crates.io does not accept packages that use git patches, so none of
  # the workspace crates can be published until Noir cuts a release that
  # matches our compiled contract artifacts.
  #
  # publish:
  #   name: Publish to crates.io
  #   needs: check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #     - name: Publish workspace crates in dependency order
  #       env:
  #         CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
  #       run: |
  #         set -euo pipefail
  #
  #         publish_with_retry() {
  #           local manifest_path="$1"
  #           local package_name="$2"
  #           local max_attempts="${3:-20}"
  #           local sleep_seconds="${4:-30}"
  #
  #           echo "Publishing ${package_name} via ${manifest_path}"
  #
  #           for attempt in $(seq 1 "${max_attempts}"); do
  #             local log_file
  #             log_file="$(mktemp)"
  #
  #             if cargo publish --manifest-path "${manifest_path}" 2>&1 | tee "${log_file}"; then
  #               rm -f "${log_file}"
  #               echo "Published ${package_name}"
  #               return 0
  #             fi
  #
  #             if grep -Eq "already exists|already uploaded" "${log_file}"; then
  #               rm -f "${log_file}"
  #               echo "${package_name} ${GITHUB_REF_NAME} already published; continuing"
  #               return 0
  #             fi
  #
  #             if grep -Eq "status 429 Too Many Requests|published too many new crates in a short period of time" "${log_file}"; then
  #               local retry_after
  #               retry_after="$(sed -n 's/.*Please try again after \(.* GMT\).*/\1/p' "${log_file}" | tail -n1)"
  #
  #               if [ -n "${retry_after}" ]; then
  #                 local target_epoch
  #                 local now_epoch
  #                 target_epoch="$(date -u -d "${retry_after}" +%s)"
  #                 now_epoch="$(date -u +%s)"
  #                 sleep_seconds="$(( target_epoch - now_epoch + 5 ))"
  #                 if [ "${sleep_seconds}" -lt 5 ]; then
  #                   sleep_seconds=5
  #                 fi
  #               fi
  #
  #               rm -f "${log_file}"
  #               echo "crates.io rate limit hit while publishing ${package_name}; retrying in ${sleep_seconds}s (${attempt}/${max_attempts})"
  #               sleep "${sleep_seconds}"
  #               continue
  #             fi
  #
  #             if grep -Eq "no matching package named|failed to select a version|not found in registry|does not exist in the registry index" "${log_file}"; then
  #               rm -f "${log_file}"
  #               echo "Dependency index not ready for ${package_name} yet; retrying in ${sleep_seconds}s (${attempt}/${max_attempts})"
  #               sleep "${sleep_seconds}"
  #               continue
  #             fi
  #
  #             echo "Publishing ${package_name} failed with a non-retryable error"
  #             cat "${log_file}"
  #             rm -f "${log_file}"
  #             return 1
  #           done
  #
  #           echo "Timed out publishing ${package_name}"
  #           return 1
  #         }
  #
  #         publish_with_retry crates/core/Cargo.toml aztec-core
  #         publish_with_retry crates/rpc/Cargo.toml aztec-rpc
  #         publish_with_retry crates/crypto/Cargo.toml aztec-crypto
  #         publish_with_retry crates/node-client/Cargo.toml aztec-node-client
  #         publish_with_retry crates/pxe-client/Cargo.toml aztec-pxe-client
  #         publish_with_retry crates/fee/Cargo.toml aztec-fee
  #         publish_with_retry crates/wallet/Cargo.toml aztec-wallet
  #         publish_with_retry crates/contract/Cargo.toml aztec-contract
  #         publish_with_retry crates/account/Cargo.toml aztec-account
  #         publish_with_retry crates/ethereum/Cargo.toml aztec-ethereum
  #         publish_with_retry Cargo.toml aztec-rs

  github-release:
    name: GitHub Release
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Generate changelog
        id: changelog
        run: |
          # Find the previous tag
          PREV_TAG=$(git tag --sort=-v:refname | sed -n '2p')
          if [ -z "$PREV_TAG" ]; then
            COMMITS=$(git log --pretty=format:"- %s (%h)" HEAD)
          else
            COMMITS=$(git log --pretty=format:"- %s (%h)" "${PREV_TAG}..HEAD")
          fi
          {
            echo "body<<EOF"
            echo "## What's Changed"
            echo ""
            echo "$COMMITS"
            echo ""
            echo "**Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREV_TAG:-$(git rev-list --max-parents=0 HEAD | head -1)}...${{ github.ref_name }}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
      - uses: softprops/action-gh-release@v2
        with:
          body: ${{ steps.changelog.outputs.body }}
          generate_release_notes: false
